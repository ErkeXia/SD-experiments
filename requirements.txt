# Core
transformers==4.44.2
accelerate==0.33.0
safetensors==0.4.3
huggingface_hub==0.24.6

# Quantization
bitsandbytes==0.43.3

# Tokenizers (fast is bundled with transformers, but SentencePiece helps with T5)
sentencepiece==0.2.0

# Nice-to-have for progress bars, etc. (optional)
tqdm>=4.66.0
